import React from "react";
import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

const Headers = () => {
  return (
    <Box sx={{ width: "100%" }}>
      <Typography variant="h1" gutterBottom>
        Welcome to MUI React App
      </Typography>
    </Box>
  );
};

export default Headers;
